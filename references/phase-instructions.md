# Phase 详细执行指令

本文件是 SKILL.md 的延伸，包含各 Phase 的完整执行指令和 agent 模板。按需读取。

---

## Phase 1 调研 Agent 模板

### 子 Agent 隔离原则

Phase 1 的每个调研 Agent **必须以真正独立的方式运行**，每个 Agent 有自己的干净 context，不受其他 Agent 和主 Agent 的影响。

**隔离模式**：使用 `delegate_task` 工具并行启动子 agent，而非在主 agent 的 context 中"并行思考"。

**工作流程**：
```
主 Agent（调度者）
    ├── delegate_task → Agent 1（核心概念，独立 context）
    ├── delegate_task → Agent 2（技术细节，独立 context）
    ├── delegate_task → Agent 3（行业案例，独立 context）
    ├── delegate_task → Agent 4（最佳实践，独立 context）
    └── delegate_task → Agent 5（资源汇总，可选）
         ↓
所有子 agent 完成后，主 agent 汇总结果
```

**每个子 agent 的指令模板**：

```
你的任务：调研 [方向描述]。

搜索方向：
1. [搜索词 1]
2. [搜索词 2]
3. [搜索词 3]
4. [搜索词 4]
5. [搜索词 5（中文信源）]

对每个搜索，WebFetch 前 2-3 个最相关结果。

输出要求：
- 写入 [项目目录]/research/0X-[方向简述].md
- 每个 agent 至少 5 条信源（4-5 个 agent 合计 ≥ 20 条）
- 每条信息标注来源 URL 和信源等级（S/A/B/C）
- 区分一手（官方文档/原始论文）vs 二手（博客/社区总结）
- 关键事实必须 ≥2 个独立信源交叉验证
- 发现矛盾直接记录，不要调和

信源等级：
- S（一手）：官方文档、原始论文、RFC、规范
- A（权威）：权威媒体、行业报告、知名作者
- B（从业者）：技术博客、教程、社区高赞回答
- C（社区）：论坛讨论、问答、评论

信息源黑名单（铁律，不可违反）：不使用百度百科、百度知道。
```

**主 Agent 在子 agent 调度时的职责**：
1. 准备每个子 agent 的 context（主题、搜索词、项目目录路径）
2. 同时发起所有子 agent（并行）
3. 等待所有子 agent 完成
4. 汇总各子 agent 的调研质量（来源数量、S/A 占比）
5. 进入 Phase 1.5 展示 Review 表格

**子 agent 完成标准**：
- 调研文件已写入 `research/` 目录
- 每个文件包含 ≥ 5 个信源
- 关键事实有交叉验证

---

## Phase 1.5 调研 Review 表格模板

所有 Agent 完成后，暂停展示调研质量摘要：

（表格中 Agent 编号对应 `research/` 目录中的文件序号：Agent 1 → `01-*.md`，Agent 2 → `02-*.md`，以此类推）

```
┌──────────────────┬──────────┬──────────┬──────────────────────────┐
│ # │ 文件         │ 来源数量  │ S/A 占比 │ 关键发现                  │
├──────────────────┼──────────┼──────────┼──────────────────────────┤
│ 1 │ 01-core.md   │ X篇      │ XX%      │ ...                       │
│ 2 │ 02-tech.md   │ X篇      │ XX%      │ ...                       │
│ 3 │ 03-cases.md  │ X篇      │ XX%      │ ...                       │
│ 4 │ 04-best.md   │ X篇      │ XX% ⚠️   │ ...                       │
│ 5 │ 05-res.md    │ X篇      │ XX%      │ ...                       │
├──────────────────┼──────────┼──────────┼──────────────────────────┤
│ 总信源           │ XX       │ S/A XX%  │ 达标 ✅ / 不达标 ⚠️        │
│ 信息缺口         │ Agent X  │          │ [缺口描述]                │
└──────────────────┴──────────┴──────────┴──────────────────────────┘
```

---

## Phase 2 风格锚定执行规范

无论哪种风格，锚定流程相同：

1. **选模板**：根据用户选择，读取 `references/style-guide.md` 中对应风格的完整模板
2. **写样板**：用第一章内容，严格按模板结构写完整一章
3. **用户确认**：展示样板章，确认：
   - 整体节奏和深度是否满意？
   - 各板块是否都需要？
   - 有没有想增减的板块？
   - 特殊排版约定（如 O'Reilly 的四字体、编号格式）是否正确？
4. **锁定**：确认后存入 `anchor-sample.md`，后续所有章节以此为基准
5. **微调**：如果用户说「X 板块去掉」「Y 板块加长」，更新模板和样板

---

## Phase 3 逐章写作上下文累积规则

- **不读全部已完成章节**（context 限制）。写第 N 章时：
  - 必读：outline.md + anchor-sample.md + glossary.md + 最近 2-3 章手稿
  - 可选：更早的章节仅在需要引用具体内容时读取
- **术语追踪**：通过 glossary.md 而非全文扫描判断术语是否已定义
- **叙事递进**：每章必须在前面章节的基础上深化，而非平行并列
- **核心论点贯穿**：全书 1-3 个核心论点必须逐章推进，不能中途丢失

**上下文累积规则：**
- Phase 3 写作时：只读取当前章 + 前 2 章手稿 + 大纲摘要（不读全部已完成章节）
- 术语追踪：维护一个 `manuscript/glossary.md`，记录已定义术语和首次出现的章节号。写新章节时只读 glossary 而非全文扫描
- Phase 4 验证时：子 agent 每次只审 3-4 章，分批审查
- 每章写完后：更新 glossary.md，新增本章节定义的术语

---

## Phase 4 质量验证详细指令

### 4a. 结构一致性检查（主 agent）

逐章检查：
- 所有章节是否遵循选定的出版风格模板？
- 章节间是否有概念重复/遗漏？
- 术语使用是否全书统一？
- 交叉引用是否正确（「如第 X 章所述」是否准确）？

### 4b. 独立质量审查（子 agent）

**spawn 独立子 agent**，带完整手稿执行审查：

```
你的任务：审查一本技术书的手稿质量。

审查维度：
1. 可读性：目标读者（{reader}）能否流畅读完？
2. 技术准确性：关键事实是否有信源支撑？有无明显错误？
3. 叙事连贯性：从头到尾是否有清晰的主线？章节间递进自然？
4. 风格一致性：各章节风格是否统一？与第一章/样板章对比有无漂移？
5. 引用质量：引用是否仍然有效？是否充分？

对比基准：{anchor-sample.md 内容}

输出格式：
- 每个维度打分（1-10）
- 列出具体问题（附章节号和原文片段）
- 给出修改建议
```

### 4c. 通过标准

| 检查项 | 通过标准 | 不通过信号 |
|--------|---------|-----------|
| 风格一致性 | 与锚定样板结构一致 | 出现样板中没有的板块或缺失 |
| 术语统一 | 同一概念全书使用同一术语 | 同一概念出现 2+ 种叫法 |
| 叙事递进 | 每章在前一章基础上深化 | 章节可随意重排顺序而不影响理解 |
| 信源覆盖 | 关键论点 ≥80% 有引用 | 核心论点凭空出现无支撑 |
| 重复率 | 章节间重复内容 < 5% | 同一概念跨章重复完整展开 |

**通过** → Phase 5。
**不通过** → 标注薄弱章节 → 回到 Phase 3 迭代（上限 2 次）。
迭代 2 次后仍有不通过项 → 在工作报告中标注薄弱维度，继续交付。

### 4d. Red Flags — 以下情况必须重写

以下情况出现任意一项 → 停止当前章节，立即重写，不进入下一项：

| 信号 | 说明 |
|------|------|
| 章节标题是"概述"、"简介"而非具体断言 | 章节标题必须是结论句 |
| 整章没有任何具体数字/时间/场景细节 | 写作内容漂浮无根 |
| 章节间出现大段重复（>5%） | 未做交叉引用整合 |
| 结尾是"以上就是本章全部内容" | 缺少承上启下的过渡 |
| O'Reilly 风格中出现"本文"/"笔者" | 风格人称错误 |

---

## Phase 5 排版 & 插图 & PDF 详细指令

### 排版规范

**所有排版参数详见 `layout.md`。** 从 layout.md 读取 CSS 参数和排版自检清单。

### 插图

**仅在 Phase 0 确认需要插图时执行。**

**所有插图参数详见 `illustration.md`。**

执行流程：
1. 规划插图数量和位置（见 illustration.md 数量表）
2. 定风格：根据主题组装 STYLE_PREFIX（见 illustration.md 主色调方案）
3. **prompt 先写入文件**：`illustrations/prompts/00-style-anchor.md`（锚定图 prompt）和 `illustrations/prompts/01-N-content.md`（其余插图 prompt）
   - 每个文件记录：`插图编号 | 位置 | 内容描述 | 比例 | 完整 prompt`
   - prompt 写完后再生成，不要边写边调
4. 锚定确认：生成 1 张锚定图 → 展示给用户确认
5. 批量生成：确认后串行生成其余插图
6. 下载压缩（见 illustration.md 压缩规则）

### PDF 生成

**使用 booksmith-typst.py 脚本**（Typst 排版引擎，原生 CJK，自动书签，单遍出 TOC）：

```bash
python3 scripts/booksmith-typst.py \
  ~/Books/[project-dir] --output [output-name].pdf
```

脚本内部完成：project.json 读取 → manuscript/*.md 合并排序 → layout.md 解析（配色、字号、边距）→ Typst 编译 PDF（封面 + 可点击 TOC 带页码 + PDF 书签 + 原生 CJK 混排）。

若 Typst 不可用（`pip install typst`），可回退到 ReportLab 版：

```bash
python3 scripts/booksmith-rl.py \
  ~/Books/[project-dir] --output [output-name].pdf
```

### Phase 5 失败处理

| 失败场景 | 处理方式 |
|---------|---------|
| CJK 字体缺失 | 检查系统是否安装 Noto Sans CJK 或 Songti SC；脚本会自动 fallback |
| Markdown 解析失败 | 检查 manuscript/*.md 文件格式是否符合 booksmith 约定（`# §01 "Title"`） |

---

## Phase 6 双 Agent 精炼详细指令

**借鉴 nuwa Phase 5 的双 Agent 精炼机制。**

**并行启动两个 Agent**：

### Agent A（内容审查视角）

```
你的任务：审查一本技术书的内容质量。

检查项目：
1. 技术细节是否有过时或不准确的地方？
2. 代码示例是否可运行？（逻辑检查，不必实际运行）
3. 引用的 URL 是否看起来仍然有效？
4. 有没有前后矛盾的说法？

输出：2-3 处具体修改建议（附原文和修改后文本）
```

### Agent B（排版审查视角）

```
你的任务：审查一本技术书的排版质量。

检查项目（对照 layout.md 排版自检清单）：
1. 所有图片是否居中且宽度一致？
2. 表格是否 width: 100%？
3. 分页是否合理（封面/目录/章节页）？
4. 插图位置是否合理（不在表格/代码块附近）？
5. 整体视觉是否干净统一？

输出：2-3 处具体修改建议（附原文和修改后 HTML）
```

**主 Agent 综合两份报告，应用不冲突的改进，展示变更摘要请用户确认。**

精炼标准：改动必须让书「读起来更流畅」，不只是增加内容。

### Phase 6 失败处理

| 失败场景 | 处理方式 |
|---------|---------|
| 精炼 Agent 建议冲突 | 展示两方建议让用户选择，不自行决定 |

---

## Phase 7 交付工作报告模板

保存到 `[项目目录]/report.md`：

```markdown
# 工作报告：[书名]

> **出版风格**：[风格]
> **完成时间**：YYYY-MM-DD
> **最终交付**：`ebook.pdf`（大小）

---

## 一、项目参数
[project.json 中的全部参数]

## 二、调研阶段
[Agent 数量、信源总数、S/A 占比、各 Agent 核心发现]

## 三、内容架构
[章节数、大纲确认记录]

## 四、风格锚定
[出版风格、样板章确认状态]

## 五、逐章写作
[每章：标题、字数、引用数、与前章的递进关系]

## 六、质量验证
[验证结果：通过/不通过项、迭代记录]

## 七、插图（如有）
[插图记录表：编号、prompt、状态、位置]

## 八、精炼记录
[Agent A/B 的修改建议及采纳情况]

## 九、关键数据

| 指标 | 数值 |
|------|------|
| 章节数 | X |
| 总字数 | X |
| 信源数 | X |
| S/A 占比 | X% |
| 插图数 | X |
| PDF 大小 | X |
| 质量验证 | 通过 ✅ / 有保留项 ⚠️ |
```

---

## 各 Phase 失败处理汇总

| Phase | 失败场景 | 处理方式 |
|-------|---------|---------|
| 1 | 信源不足（<10 条） | 告知用户降低期望，补充本地语料或调整主题范围 |
| 1 | 子 agent 超时/无结果 | 跳过该维度，在调研文件中标注「信息不足」，Phase 1.5 展示缺口 |
| 2 | 用户反复否决样板章（>3 次） | 暂停，请用户提供一段「理想风格」的参考文本 |
| 3 | 定向补充搜索无结果 | 基于已有调研材料写，标注「此处数据待补充」 |
| 4 | 质量验证不通过（2 次迭代后） | 标注薄弱章节和维度，继续交付而非无限循环 |
| 5 | PDF 生成失败 | 检查 `typst` 是否安装（`pip install typst`）、layout.md 格式、manuscript/*.md 编码；检查系统 CJK 字体；回退到 `booksmith-rl.py` |
| 6 | 精炼 Agent 建议冲突 | 展示两方建议让用户选择，不自行决定 |

---

## 章节字数目标（按出版风格）

| 出版风格 | 单章目标字数 | 代码占比 | 类比/场景密度 |
|---------|-----------|---------|------------|
| O'Reilly | 3000-5000 字 | 15-25% | 每个核心概念至少 1 个类比 |
| Academic | 4000-6000 字 | 5-15% | 少类比，多用数据和引用 |
| Handbook | 2000-4000 字 | 30-50% | 无类比，步骤 + 截图描述 |

**全书总字数估算** = 章节数 × 单章目标字数。Phase 0 时即告知用户预期总字数。
